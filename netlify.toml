[build]
  publish = "public"

[context.master]
  # masterブランチ（公開ブランチ）の設定
  command = "npm run build"
  environment = { NODE_ENV = "production" }

[context.develop]
  # developブランチ（開発ブランチ）の設定
  command = "mv _headers static/_headers && npm run build"
  environment = { NODE_ENV = "development" }
