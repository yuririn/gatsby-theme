[build]
  publish = "public"
  command = "npm run build"

[context.master]
  NODE_ENV = "development"

[context.production]
  NODE_ENV = "development"

[context.branch-deploy]
  NODE_ENV = "development"
