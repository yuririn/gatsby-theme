[build]
  publish = "public"
  command = "npm run build"

[context.master]
  environment = { NODE_ENV = "production" }

[context.develop]
  environment = { NODE_ENV = "development" }

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/add-headers"
  status = 200
  force = true
