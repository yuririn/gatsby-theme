[build]
  publish = "public"
  command = "npm run build && npm run update-headers"

[context.master]
  NODE_ENV = "production"

[context.deploy]
  NODE_ENV = "development"

[context.branch-deploy]
  NODE_ENV = "development"
